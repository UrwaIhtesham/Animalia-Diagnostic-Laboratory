import React from 'react';
import './loading.css';

const Loading = () => {
    return (
        <div className='loader'>
            <h1 className='load-h1'>Loading</h1>
        </div>
    );
};

export default Loading;